# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-de1soc-handoff"
BBFILE_PATTERN_meta-de1soc-handoff = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-de1soc-handoff = "6"

LAYERDEPENDS_meta-de1soc-handoff = "core"
LAYERSERIES_COMPAT_meta-de1soc-handoff = "scarthgap"
